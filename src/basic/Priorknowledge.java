package basic;

/**
 * String의 간단한 사전지식
 */
public class Priorknowledge {


    /**
     * charAt(i) : 찾을 문자열의 위치에 해당되는 문자를 리턴해준다.
     * toCharArray(str) : str문자열을 char[]로 변환하여준다.
     * IndexOf(str): str이 위치를 알려준다. (str, int)처럼 특정위치에서 시작해서 검색할 수도 있다.
     * substring(begin, end): 문자열의 begin 부터 end 위치까지의 문자를 잘라서 리턴한다.
     * str.startWith(c): c가 문자열의 첫번째 문자와 일치하는지 아닌지 알려준다.
     * str.endWith(c): c가 str의 첫번째 문자와 일치하는지 아닌지 알려준다.
     * Character.isDigit(c) : c가 숫자형인지 아닌지 알려준다. true/false
     * Character.isLetter(c) : c가 문자형인지 아닌지 알려준다. true/false
     *
     */

}
